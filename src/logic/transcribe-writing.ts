import { verbose } from "src/logic/utils/universal-dev-logging";

///////////////////
///////////////////

/**
 * Transcribes handwriting from a writing file.
 * Stub: real handwriting transcription is not wired yet.
 */
export async function transcribeWriting(): Promise<string> {
    verbose('Handwriting transcription is still a stub');
    return 'transcribed';
}
