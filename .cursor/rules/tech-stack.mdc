---
description: Tech stack — libraries, frameworks, and tools in use
alwaysApply: true
---

# Tech Stack

This file is the source of truth for the libraries, frameworks, and tools used in this project. **Keep it up to date — updating this file is the final step of every plan that introduces, removes, or changes a dependency or tool.**

---

- **Language / platform:** TypeScript, Obsidian API, CodeMirror 6 (Live Preview widgets)
- **Drawing / writing UI:** tldraw v2.x
- **SVG metadata (parse/serialize):** `@xmldom/xmldom` (not deprecated `xmldom`)
- **SVG rasterization:** `canvg` ^4.0.3 (prototype-pollution fix vs 4.0.1)
- **Dev console coloring:** Chromium `%c` styles in `universal-dev-logging.ts` (no chalk)
- **Testing:** Jest (unit/integration), WebdriverIO + wdio-obsidian-service (e2e)
- **IDs:** `nanoid` ^5.1.6
- **Almost Useful account:** portal `/oauth/authorize` + `POST /api/oauth/token` (app JWT `typ=almostuseful_app`). No `@supabase/supabase-js` / anon key in the plugin. Tokens in device-local `saveLocally('almostuseful_session')` (`au_ink_almostuseful_session`).
- **Build tooling:** esbuild (plugin bundling), npm scripts
- **Linting:** ESLint flat config (`eslint.config.mjs`) + `eslint-plugin-obsidianmd` ^0.4.2
- **Boox companion:** eInk Bridge Android app (`eink-bridge` repo) — loopback WebSocket on the same tablet
